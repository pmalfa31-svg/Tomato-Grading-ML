/*
 * Questo file va compilato con il compilatore C (Arduino/PlatformIO lo fa
 * automaticamente per i file .c nella cartella dello sketch), perche'
 * tomato_classifier.h contiene sintassi C99 (compound literal) non valida
 * in C++. Vedi il commento @warning in cima a tomato_classifier.h e la
 * sezione "Compatibilita' C vs C++" nel README del progetto.
 */
#include "tomato_classifier.h"
